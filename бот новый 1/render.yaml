services:
  - type: worker
    name: polymarket-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: MARKET_SLUG
        sync: false
      - key: POLY_PRIVATE_KEY
        sync: false
      - key: DRY_RUN
        value: "1"
